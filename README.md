# 🎮 Valorant Team Search

Telegram бот для пошуку напарників у гру Valorant з системою модерації анкет.

## ✨ Функціонал

### Для користувачів:
- 📝 Створення детальних анкет через покроковий діалог
- 🎯 Вибір рангу, ролей, агентів, серверів
- 🌍 Підтримка всіх регіонів Valorant
- 👀 Перегляд та управління власною анкетою
- ⏰ Автоматичне обмеження частоти створення анкети

### Для модераторів:
- ✅ Схвалення анкет з автоматичною публікацією в канал
- ❌ Відхилення анкет з вибором причин або власним текстом
- 🔄 Повний процес модерації з підтвердженням

### Для власника:
- 👥 Управління модераторами (додавання/видалення)
- 📋 Перегляд списку модераторів
- 🔐 Перевірка прав доступу

## 🚀 Швидкий старт

### 1. Встановлення

```bash
# Клонувати репозиторій
git clone https://github.com/Glerik23/ValorantTeamSearch.git
cd ValorantTeamSearch

# Створити віртуальне середовище
python -m venv venv

# Активувати віртуальне середовище
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Встановити залежності
pip install -r requirements.txt
```

### 2. Налаштування

Створіть файл `.env` на основі `.env.example`:

```bash
cp .env.example .env
```

Заповніть необхідні змінні:

```env
BOT_TOKEN=your_bot_token_from_botfather
BOT_OWNER_ID=your_telegram_id
MODERATOR_CHAT_ID=moderators_chat_id
PUBLIC_CHANNEL_ID=your_channel_chat_id чи @your_channel       # опційно
```

Важливі налаштування в config.py:
```python
# Обмеження для бази даних
MAX_RIOT_ID_LENGTH = 50       # Максимальна довжина Riot ID
MAX_RANK_LENGTH = 20          # Максимальна довжина рангу
MAX_ROLE_LENGTH = 100         # Максимальна довжина ролей
MAX_BIO_LENGTH = 500          # Максимальна довжина "про себе"
MAX_CONTACT_LENGTH = 25       # Максимальна довжина контакту
MAX_USERNAME_LENGTH = 50      # Максимальна довжина username
MAX_STATUS_LENGTH = 20        # Максимальна довжина статусу

# Обмеження вибору
MAX_AGENTS_SELECTION = 8      # Максимум агентів в анкеті
MAX_ROLES_SELECTION = 4       # Максиму м ролей в анкеті
```

#### Як отримати дані:

1. **BOT_TOKEN**: 
   - Напишіть [@BotFather](https://t.me/botfather)
   - Створіть нового бота командою `/newbot`
   - Скопіюйте токен

2. **BOT_OWNER_ID**: 
   - Напишіть [@userinfobot](https://t.me/userinfobot)
   - Скопіюйте ваш ID

3. **MODERATOR_CHAT_ID** (опційно):
   - Створіть групу для модераторів
   - Додайте [@userinfobot](https://t.me/userinfobot) в групу
   - Скопіюйте ID групи

4. **PUBLIC_CHANNEL_ID** (опційно):
   - Створіть публічний канал
   - Додайте бота як адміністратора
   - Використайте @username або ID каналу

### 3. Запуск

```bash
python main.py
```

Якщо все налаштовано правильно, ви побачите в консолі:
```
2025-01-XX XX:XX:XX - __main__ - INFO - ==================================================
2025-01-XX XX:XX:XX - __main__ - INFO - Запуск бота...
2025-01-XX XX:XX:XX - __main__ - INFO - Таблиці бази даних перевірено/створено
2025-01-XX XX:XX:XX - __main__ - INFO - Роутери зареєстровано
2025-01-XX XX:XX:XX - __main__ - INFO - Бот запущено та готовий до роботи!
```

Логи також зберігаються в файл `logs/bot.log`.

## 📋 Команди

### Для всіх користувачів:
- `/start` - Запустити бота
- `/cancel` - Скасувати поточну дію (створення анкети тощо)
- Кнопка "Подати анкету" - Створити нову анкету
- Кнопка "Моя анкета" - Переглянути свою анкету
- Кнопка "Правила" - Переглянути правила

### Для власника:
- `/add_moderator <user_id або @username>` - Додати модератора
- `/remove_moderator <user_id або @username>` - Видалити модератора
- `/list_moderators` - Список всіх модераторів
- `/check_my_rights` - Перевірити свої права

### Для модераторів:
- Схвалення/відхилення анкет через інлайн-кнопки

### 🔄 Процес роботи з анкетами

Створення анкети:
- Натисніть "Подати анкету"
- Заповніть всі поля (Riot ID, вік, ранг, ролі, агенти, сервери, про себе, контакти)
- Підтвердіть анкету
- Зачекайте модерацію

Якщо анкету відхилено:
- ✅ Ви можете негайно створити нову анкету
- 📝 Врахуйте зауваження модератора


## 🛡️ Модераторський чат

### Призначення:
- Чат для модерації анкет (ID задається в `MODERATOR_CHAT_ID`)
- Тільки управління заявками та адміністративні команди

### Обмеження в модераторському чаті:
- ❌ **Не відображаються кнопки головного меню**
- ❌ **Не можна створювати анкети**
- ❌ **Не відображаються правила**
- ❌ **Не працює команда /start** (тільки інформаційне повідомлення)
- ✅ **Працюють інлайн-кнопки модерації**
- ✅ **Власник може використовувати адміністративні команди**
- ✅ **Модератори можуть перевіряти свої права**

### Доступні команди в модераторському чаті:

#### Для всіх модераторів:
- `/check_my_rights` - перевірити свої права
- Інлайн-кнопки під повідомленнями з анкетами

#### Тільки для власника:
- `/add_moderator` - додати модератора
- `/remove_moderator` - видалити модератора  
- `/list_moderators` - список модераторів

## ❓ FAQ 

Q: Чому я не можу створити анкету? \
A: У вас вже є активна анкета (на модерації або опублікована). Видаліть поточну анкету через "Моя анкета".

Q: Що відбувається при відхиленні анкети? \
A: Анкета повністю видаляється з системи. Ви отримаєте сповіщення з причиною і зможете створити нову.

Q: Як оновити дані в анкеті? \
A: Видаліть поточну анкету і створіть нову з актуальними даними.

Q: Модератор не бачить мою анкету? \
A: Анкети з'являються в черзі модерації відразу після створення. Якщо минуло багато часу, зверніться до адміністратора.

Q: Повідомлення в каналі не видаляється? \
A: Це рідкісна помилка. Повідомте адміністратору ID анкети для ручного видалення.

## 🗂 Структура проєкту

```
.
├── config.py              # Конфігурація бота
├── main.py                # Точка входу
├── requirements.txt       # Залежності
├── .env                   # Змінні оточення (не в git)
├── .env.example          # Приклад змінних
├── .gitignore            # Git ignore
├── logs/                  # Папка з логами (створюється автоматично)
│   └── bot.log           # Файл логів
├── db/
│   ├── models.py         # Моделі бази даних
│   └── requests.py       # Запити до БД
├── handlers/
│   ├── user_handlers.py  # Обробники для користувачів
│   └── admin_handlers.py # Обробники для модераторів
└── keyboards/
    ├── reply.py          # Reply клавіатури
    └── inline.py         # Inline клавіатури
```

## 🔧 Налаштування

### Обмеження тексту:
- **Riot ID**: Формат Name#TAG (перевірка формату)
- **Вік**: 13-100 років
- **Біографія**: До `MAX_BIO_LENGTH` символів (або пропустити, відправивши '-')
- **Контакт**: До `MAX_CONTACT_LENGTH` символів

## 🔒 Безпека

- Всі текстові поля екрануються через `html.escape()`
- Валідація всіх введених даних
- Обмеження частоти створення анкет
- Система прав доступу

## 📝 Логування

Система логування налаштована автоматично та працює з моменту запуску бота:

### Що логується:
- ✅ Запуск/зупинка бота
- ✅ Створення та модерація анкет 
- ✅ Додавання/видалення модераторів
- ✅ Помилки при роботі з API Telegram
- ✅ Важливі події (створення анкет, схвалення, відхилення)

### Де зберігаються логи:
- 📄 **Файл**: `logs/bot.log` - всі логи зберігаються тут
- 💻 **Консоль**: логи також виводяться в консоль для моніторингу

### Ротація логів:
- Автоматична ротація при досягненні 5MB
- Зберігається до 3 резервних копій (`bot.log`, `bot.log.1`, `bot.log.2`, `bot.log.3`)
- Кодування: UTF-8

### Формат логів:
```
2025-01-XX XX:XX:XX - module_name - LEVEL - message
```

Приклад:
```
2025-01-15 14:30:25 - handlers.user_handlers - INFO - Анкета #42 створена користувачем 123456789 (@username) та відправлена на модерацію
```

## 🐛 Troubleshooting

### Бот не запускається:
1. Перевірте `.env` файл (створіть його вручну, якщо потрібно)
2. Переконайтеся, що BOT_TOKEN та BOT_OWNER_ID заповнені
3. Перевірте помилки в консолі

### Користувач не може створити анкету:
1. Перевірте через "Моя анкета" чи є активна анкета
2. Видаліть її якщо є
3. Перевірте логи бота якщо немає

### Анкети не публікуються:
1. Перевірте PUBLIC_CHANNEL_ID
2. Переконайтеся, що бот є адміністратором каналу
3. Перевірте, що у бота є права на публікацію

### Модератор не може схвалювати:
1. Переконайтеся, що користувач доданий командою `/add_moderator`
2. Перевірте права командою `/check_my_rights`


## 🤝 Внесок:

**Pull requests вітаються! Для великих змін спочатку створіть issue.**

## 📄 Ліцензія:

**MIT License**

## 🔧 Технології використано:

<div align="left">
  <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/python/python-original.svg" height="40" alt="python logo"  />
  <img width="12" />
  <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/sqlite/sqlite-original.svg" height="40" alt="sqlite logo"  />
  <img width="12" />
  <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/sqlalchemy/sqlalchemy-original.svg" height="40" alt="sqlalchemy logo"  />
  <img width="12" />
  <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/pycharm/pycharm-original.svg" height="40" alt="pycharm logo"  />
</div>
